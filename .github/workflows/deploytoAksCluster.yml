on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n jmkubcr -g jmkubcr81e2-rg --location "East US" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n jmkubcr --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n jmkubcr --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n jmkubcr --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: jmkubcr.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t jmkubcr.azurecr.io/jmkub:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push jmkubcr.azurecr.io/jmkub:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_jmkub_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep jm-kub9667 | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace jm-kub9667`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: jm-kub9667
        container-registry-url: jmkubcr.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: jmkubdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: jm-kub9667
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          jmkubcr.azurecr.io/jmkub:${{ github.sha }}
        imagepullsecrets: |
          jmkubdockerauth   
